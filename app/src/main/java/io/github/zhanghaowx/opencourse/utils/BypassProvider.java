package io.github.zhanghaowx.opencourse.utils;

/**
 * A singleton class that providers bus to whole application
 */
public class BypassProvider {
    /*
    private static Bypass mBypass;

    public static Bypass getBypass() {
        if (mBypass == null) {
            synchronized (Bypass.class) {
                if (mBypass == null) {
                    mBypass = new Bypass();
                }
            }
        }
        return mBypass;
    }
    */
}

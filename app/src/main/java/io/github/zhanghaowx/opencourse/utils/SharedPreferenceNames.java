package io.github.zhanghaowx.opencourse.utils;

/**
 * Holds names for all information stored in user shared preferences
 */
public class SharedPreferenceNames {
    /**
     * For design guidelines, you should show the drawer menu until the user expands it manually
     */
    public static final String PREF_USER_LEARNED_DRAWER = "navigation_drawer_learned";
    /**
     * Store user session id if user already logged in
     */
    public static final String PREF_USER_SESSION_ID = "user_session_id";
}
